non-linear
    Configs for default non-linear search (e.g. MCMC, nested sampling) settings.
notation
    Configs defining labels and formatting of model parameters when used for visualization.
priors
    Configs defining default priors assumed on every example model component and set of parameters.
visualize
    Configs defining what images are output by a model fit.